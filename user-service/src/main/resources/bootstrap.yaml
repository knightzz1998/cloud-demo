spring:
  application:
    name: user-service # 服务名称
  profiles:
    active: dev # 激活的环境
  cloud:
    nacos:
      server-addr: localhost:8848 # nacos注册中心地址
      config:
        file-extension: yaml # 文件后缀名